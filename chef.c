#include<stdio.h>
int main(){
int t,w,x,y,z,p,q,r,s,A;
scanf("%d",&t);
while(t--){
    scanf("%d%d%d%d",&w,&x,&y,&z);
    scanf("%d%d%d%d",&p,&q,&r,&s);
    scanf("%d",&A);
    if(p>=w && q>=x && r>=y && s>=z){
        while( (p-w)>=1 && (q-x)>=1 && (r-y)>=1 && (s-z)>=1 ){
            
        }
    }
}
return 0;
}